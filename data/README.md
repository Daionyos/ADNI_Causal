# Data Directory

Place `ADNIMERGE.csv` here after obtaining it from the ADNI data portal under
the ADNI data use agreement. The raw ADNI file is not included in this
repository and should not be committed.

Ontology embedding files used by Onto-CGAN are also local runtime inputs and
are not committed here.
